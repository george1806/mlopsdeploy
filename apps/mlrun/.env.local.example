# --- Harbor (offline registry) ---
HARBOR_URL=harbor.local:30002
HARBOR_PROJECT=mlrun
HARBOR_USER=
HARBOR_ADMIN_PASSWORD=

# --- K8s release/namespace ---
MLRUN_NAMESPACE=mlrun
MLRUN_RELEASE_NAME=mlrun

INSTALL_MODE=harbor   # harbor/local

# --- Chart selection ---
# If empty, installer reads apps/mlrun/offline-meta/charts.lock (mlrun-ce=<ver>)
MLRUN_CHART_VERSION=
MLRUN_VERSION=0.9.0

# --- Image tags (MUST exist in Harbor; your prep/push phases should mirror these) ---
# Core MLRun
MLRUN_TAG=1.9.1
MLRUN_UI_TAG=1.9.1
MLRUN_JUPYTER_TAG=1.9.1          # if you keep mlrun’s jupyter tag; or keep generic Jupyter tag below

# Nuclio
NUCLIO_TAG=1.13.24-amd64

# MinIO (server + mc)
MINIO_TAG=RELEASE.2024-04-18T19-09-19Z
MINIO_MC_TAG=RELEASE.2024-04-18T16-45-29Z

# Jupyter (generic base)
JUPYTER_TAG=1.9.1

# Kafka, TDengine, Grafana, Prometheus stack, KFP, Spark, MPI, MySQL, MLMD
KAFKA_TAG=3.9.0-debian-12-r6
TDENGINE_TAG=3.0.7.1
GRAFANA_TAG=9.2.2
PROMETHEUS_TAG=v2.39.1
PROM_OPERATOR_TAG=v0.60.1
NODE_EXPORTER_TAG=v1.3.1
KSM_TAG=v2.6.0
KFP_API_SERVER_TAG=2.5.0
KFP_METADATA_WRITER_TAG=2.5.0
KFP_PERSISTENCE_AGENT_TAG=2.5.0
KFP_SCHEDULED_WF_CTRL_TAG=2.5.0
KFP_VIEWER_CRD_CTRL_TAG=2.5.0
SPARK_OPERATOR_TAG=2.1.0
MPI_OPERATOR_TAG=v0.2.3-igz
MYSQL_TAG=8.0.26
MLMD_TAG=1.16.0
KIWIGRID_SIDECAR_TAG=1.19.2
BATS_TAG=v1.4.1

# --- Storage (tune for prod) ---
DEFAULT_STORAGE_CLASS=local-path
MLRUN_DB_SIZE=20Gi
MLRUN_ARTIFACTS_SIZE=50Gi
MINIO_SIZE=100Gi

# --- Optional public hostnames (Traefik) ---
MLRUN_UI_HOST=mlrun.local
JUPYTER_HOST=jupyter.local
MINIO_HOST=minio.local

# --- Strict validation: fail install if any image not mapped to Harbor (true/false) ---
STRICT_IMAGE_VALIDATION=true
